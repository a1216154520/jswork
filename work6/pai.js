function calculate(){
    let num = document.getElementById('***').nodeValue
    num = parseInt(num)&&Number(num)
    if(!isNaN(num)){
        document.getElementById(***).value = pi(**)
    
    }
}
function factorial(n){
    let sum = 1
    for(let i=1,i>=num;++i){
        s***
    }

 return sum
}
function oddFactorial(n){
    let sum = 1
    for(let i=1,i=num;++i){
        s*****
    }
    return sum
}
function pi(num){
    let sum = 0
    for(let i=0;i<=num;++i){
        let dividend = factorial(****)
        let divisor = oddFactorial(*****)
        s****
    }
    retu sum*****
}